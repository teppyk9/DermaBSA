/**
 * Pacchetto delle classi di utilità.
 *
 * Contiene [OnnxHelper] per l'inferenza del modello di segmentazione
 * e [BsaCalculator] per i calcoli BSA e PASI puri (senza dipendenze Android).
 */
package com.uninsubria.derma_bsa.util
