/**
 * Pacchetto principale dell'applicazione DermaBSA.
 *
 * Contiene l'Activity principale ([MainActivity]) e il ViewModel condiviso
 * ([AppViewModel]) che mantiene lo stato dell'intera sessione di misurazione.
 */
package com.uninsubria.derma_bsa
